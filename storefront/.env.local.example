###
# Example environment file for the Next.js storefront
#
# Copy this file to `.env.local` in the `storefront` folder and replace
# the placeholders with your actual values.  The only required variable is
# `NEXT_PUBLIC_MEDUSA_BACKEND_URL`, which tells the storefront where to
# find your Medusa backend API.  See the Medusa environment variables
# documentation【611812571810596†L446-L479】 for more details on configuring the backend.

# The base URL of your Medusa backend.  Use the full URL including protocol
# and port (if non‑standard).  Do **not** include the `/app` suffix – this
# should point to the API root.  Example:
#   NEXT_PUBLIC_MEDUSA_BACKEND_URL=https://admin.eww-pew.com
NEXT_PUBLIC_MEDUSA_BACKEND_URL=https://admin.eww-pew.com

# If your storefront integrates with third‑party services or plugins, you can
# define additional environment variables here.  For example, if you use a
# search provider or payment gateway, consult the corresponding plugin
# documentation for the required variables.