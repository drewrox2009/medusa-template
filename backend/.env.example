###
# Example environment file for the Medusa backend
#
# Copy this file to `.env` and replace the placeholder values with your
# actual secrets and connection strings.  For more details see the
# general deployment guide【611812571810596†L446-L477】 and the Minio plugin
# documentation【335184576063223†L171-L180】.

# -----------------------------------------------------------------------------
# Secrets
# Generate secure random strings for these values.
COOKIE_SECRET=replace-with-random-cookie-secret
JWT_SECRET=replace-with-random-jwt-secret

# -----------------------------------------------------------------------------
# Application mode and port
MEDUSA_WORKER_MODE=server        # run in server mode to handle API and admin
PORT=9000                        # port the backend listens on inside the container
NODE_ENV=production

# -----------------------------------------------------------------------------
# Database and cache configuration
# Provide the full connection URL for your PostgreSQL database.  Example:
# postgres://user:password@db.example.com:5432/medusa
DATABASE_URL=postgres://<username>:<password>@<host>:<port>/<database>

# Provide your Redis connection URL.  Example:
# redis://cache.example.com:6379
REDIS_URL=redis://<host>:<port>

# -----------------------------------------------------------------------------
# File storage (Minio/S3)
# To enable file uploads (e.g. product images) you can use the `medusa-file-minio`
# plugin.  Set these variables to point to your Minio installation.  See
# https://docs.medusajs.com/v1/plugins/file-service/minio for details【335184576063223†L171-L180】.
MINIO_ENDPOINT=http://<minio-host>:9000
MINIO_BUCKET=medusa-media
MINIO_ACCESS_KEY=<minio-access-key>
MINIO_SECRET_KEY=<minio-secret-key>

# Optional: private bucket for exports/imports
#MINIO_PRIVATE_BUCKET=medusa-private
#MINIO_PRIVATE_ACCESS_KEY=<private-access-key>
#MINIO_PRIVATE_SECRET_KEY=<private-secret-key>

# -----------------------------------------------------------------------------
# CORS configuration
# These URLs are used by Medusa to validate cross‑origin requests from the
# storefront and admin.  Update them to match the domains you will use.
STORE_CORS=https://store.eww-pew.com
ADMIN_CORS=https://admin.eww-pew.com
AUTH_CORS=https://store.eww-pew.com,https://admin.eww-pew.com

# -----------------------------------------------------------------------------
# Backend URL used by the admin configuration
# This should point to the public URL of your backend (without the /app suffix).
MEDUSA_BACKEND_URL=https://admin.eww-pew.com

# -----------------------------------------------------------------------------
# Admin build
# Set this to `false` to enable the admin UI being built and served at
# `/app` (recommended for a combined backend/admin container).  If you choose
# to build and serve the admin separately, set this to `true`.
DISABLE_MEDUSA_ADMIN=false